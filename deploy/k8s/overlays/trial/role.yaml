apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: evidra-events-reader
  labels:
    app.kubernetes.io/name: evidra
rules:
  - apiGroups: [""]
    resources: ["events"]
    verbs: ["get", "list", "watch"]
