apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: evidra-data
  labels:
    app.kubernetes.io/name: evidra
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
