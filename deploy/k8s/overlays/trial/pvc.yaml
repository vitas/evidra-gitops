apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: evidra-gitops-data
  labels:
    app.kubernetes.io/name: evidra-gitops
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
