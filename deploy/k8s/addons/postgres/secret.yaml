apiVersion: v1
kind: Secret
metadata:
  name: postgres
  labels:
    app.kubernetes.io/name: postgres
type: Opaque
stringData:
  # Addon defaults for local/lab use. Override for shared or production clusters.
  POSTGRES_DB: evidra
  POSTGRES_USER: evidra
  POSTGRES_PASSWORD: change-me
