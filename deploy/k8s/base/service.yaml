apiVersion: v1
kind: Service
metadata:
  name: evidra
  labels:
    app.kubernetes.io/name: evidra
spec:
  selector:
    app.kubernetes.io/name: evidra
  ports:
    - name: http
      port: 80
      targetPort: http
