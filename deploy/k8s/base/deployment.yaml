apiVersion: apps/v1
kind: Deployment
metadata:
  name: evidra-gitops
  labels:
    app.kubernetes.io/name: evidra-gitops
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: evidra-gitops
  template:
    metadata:
      labels:
        app.kubernetes.io/name: evidra-gitops
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: evidra-gitops
          image: evidra-gitops:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          envFrom:
            - configMapRef:
                name: evidra-gitops-config
            - secretRef:
                name: evidra-gitops-secrets
          volumeMounts:
            - name: evidra-gitops-data
              mountPath: /var/evidra-gitops
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
      volumes:
        - name: evidra-gitops-data
          emptyDir: {}
